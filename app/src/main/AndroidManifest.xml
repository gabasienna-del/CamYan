<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gaba.eskukap">

    <!-- Разрешения на фото -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:targetApi="33" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:label="CamYan"
        android:theme="@style/AppTheme">

        <!-- Xposed meta-data -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="CamYan selfie helper" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />

        <!-- FakePhotoProvider -->
        <provider
            android:name=".provider.FakePhotoProvider"
            android:authorities="com.gaba.eskukap.fakephoto"
            android:exported="true"
            android:grantUriPermissions="true" />

        <!-- Окно настроек модуля для LSPosed -->
        <activity
            android:name=".ui.CamYanSettingsActivity"
            android:theme="@style/AppTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- важно: категория настроек Xposed -->
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

    </application>

</manifest>
