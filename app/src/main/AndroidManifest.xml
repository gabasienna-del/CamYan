<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gaba.eskukap">

    <application
        android:allowBackup="false"
        android:supportsRtl="true"
        android:label="Eskukap">

        <meta-data
            android:name="xposedmodule"
            android:value="true"/>

        <meta-data
            android:name="xposeddescription"
            android:value="Eskukap camera hook"/>

        <meta-data
            android:name="xposedminversion"
            android:value="93"/>

        <!-- Активность настроек модуля -->
        <activity
            android:name="com.gaba.eskukap.SettingsActivity"
            android:exported="true"
            android:label="Eskukap Settings"/>

    </application>
</manifest>
